'use strict';

// Declare app level module which depends on filters, and services
angular.module('yacy', ['ngRoute', 'ui.bootstrap', 'ngResource', 'ngCookies', 'angularLocalStorage', 'yacy.filters', 'yacy.services', 'yacy.directives', 'yacy.controllers']).
  config(['$routeProvider', function($routeProvider) {
    }]);

'use strict';


// Declare app level module which depends on filters, and services
angular.module('yacy', ['ui.bootstrap', 'localStorage', 'yacy.filters', 'yacy.services', 'yacy.directives', 'yacy.controllers']).
    config(['$routeProvider', function($routeProvider) {
    }]);
